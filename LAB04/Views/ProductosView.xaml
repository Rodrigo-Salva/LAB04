<UserControl x:Class="LAB04.Views.ProductosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:LAB04.ViewModels"
             xmlns:conv="clr-namespace:LAB04.Converters"
             mc:Ignorable="d" d:DesignWidth="1150" d:DesignHeight="600"
             d:DataContext="{d:DesignInstance Type=vm:ProductosViewModel}">
    <UserControl.Resources>
        <conv:BoolAErrorBrushConverter x:Key="BoolAErrorBrush"/>
    </UserControl.Resources>
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="16"/>
            <ColumnDefinition Width="320"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Style="{StaticResource Tarjeta}">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="Productos" Style="{StaticResource TituloSeccion}"/>
                <DataGrid ItemsSource="{Binding Productos}" SelectedItem="{Binding ProductoSeleccionado}"
                          AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Id" Binding="{Binding IdProducto}" Width="40"/>
                        <DataGridTextColumn Header="Nombre" Binding="{Binding NombreProducto}" Width="160"/>
                        <DataGridTextColumn Header="Categoría" Binding="{Binding NombreCategoria}" Width="120"/>
                        <DataGridTextColumn Header="Proveedor" Binding="{Binding NombreProveedor}" Width="150"/>
                        <DataGridTextColumn Header="Precio" Binding="{Binding PrecioUnidad, StringFormat=C}" Width="80"/>
                        <DataGridTextColumn Header="Stock" Binding="{Binding UnidadesEnExistencia}" Width="60"/>
                        <DataGridCheckBoxColumn Header="Descontinuado" Binding="{Binding Descontinuado}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>

        <Border Grid.Column="2" Style="{StaticResource Tarjeta}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Mantenimiento" Style="{StaticResource TituloPanel}"/>
                    <TextBlock Text="Nombre" Style="{StaticResource Etiqueta}"/>
                    <TextBox Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="Categoría" Style="{StaticResource Etiqueta}"/>
                    <ComboBox ItemsSource="{Binding Categorias}" SelectedItem="{Binding CategoriaCombo}"
                              DisplayMemberPath="NombreCategoria"/>
                    <TextBlock Text="Proveedor" Style="{StaticResource Etiqueta}"/>
                    <ComboBox ItemsSource="{Binding Proveedores}" SelectedItem="{Binding ProveedorCombo}"
                              DisplayMemberPath="NombreCompania"/>
                    <TextBlock Text="Cantidad por unidad" Style="{StaticResource Etiqueta}"/>
                    <TextBox Text="{Binding CantidadPorUnidad, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="Precio unitario" Style="{StaticResource Etiqueta}"/>
                    <TextBox Text="{Binding PrecioTexto, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="Unidades en existencia" Style="{StaticResource Etiqueta}"/>
                    <TextBox Text="{Binding ExistenciaTexto, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="Unidades en pedido" Style="{StaticResource Etiqueta}"/>
                    <TextBox Text="{Binding EnPedidoTexto, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="Nivel de reorden" Style="{StaticResource Etiqueta}"/>
                    <TextBox Text="{Binding NivelReordenTexto, UpdateSourceTrigger=PropertyChanged}"/>
                    <CheckBox Content="Descontinuado" IsChecked="{Binding Descontinuado}" Margin="0,10,0,0"/>
                    <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                        <Button Content="Nuevo" Margin="0,0,6,0" Command="{Binding NuevoCommand}"/>
                        <Button Content="Guardar" Style="{StaticResource BotonPrimario}" Margin="0,0,6,0" Command="{Binding GuardarCommand}"/>
                        <Button Content="Eliminar" Style="{StaticResource BotonPeligro}" Command="{Binding EliminarCommand}"/>
                    </StackPanel>
                    <TextBlock Text="{Binding Mensaje}" Foreground="{Binding EsError, Converter={StaticResource BoolAErrorBrush}}"
                               TextWrapping="Wrap" Margin="0,12,0,0"/>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
